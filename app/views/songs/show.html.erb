

<h1>Song Name: <%= @song.name %></h1>
<h3>Artist: <%= link_to @song.artist.name, artist_path(@song.artist.id)%></h3>
<h3>Genre: <%= link_to @song.genre.name, genre_path(@song.genre.id) %></h3>

<p>Menu: </p>
<div><%= link_to "Edit this Song", edit_song_path(@song) %></div>

<div><%= link_to "Return to Songs Index", songs_path %></div>
